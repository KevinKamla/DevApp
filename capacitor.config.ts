import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gecinformatique.devapp',
  appName: 'Devapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
